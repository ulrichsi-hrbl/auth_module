import 'package:flutter/material.dart';

class CreateAccountPanel extends StatelessWidget {
  const CreateAccountPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      mainAxisSize: MainAxisSize.min,
      children: [
        SizedBox(
          height: 20,
        ),
        Text('TODO: create account'),
        SizedBox(
          height: 20,
        ),
      ],
    );
  }
}
